# Label

A plain `<label>` in the UI type size. Associate it with `htmlFor` or by nesting the control.
Prefer [Field](/docs/components/field), which does this and adds description and error text.

```tsx
<Label htmlFor="name">Name</Label>
```
