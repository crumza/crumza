# Textarea

Multi-line text with the same field treatment as [Input](/docs/components/input). Resizes vertically.

```tsx
<Textarea id="summary" aria-invalid="true" />
```

## Props

Extends `ComponentProps<'textarea'>`.
